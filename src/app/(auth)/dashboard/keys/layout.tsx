import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'API Keys | DocuExtract',
};

export default function KeysLayout({ children }: { children: React.ReactNode }) {
  return children;
}
