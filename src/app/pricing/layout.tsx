import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Pricing | DocuExtract',
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
