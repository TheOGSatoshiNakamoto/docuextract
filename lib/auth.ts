// TODO (Phase 1, task: Build API key auth middleware)
// Implements authenticateRequest(req) — validates Bearer token against Supabase
// and returns the authenticated User with plan/limits.
export {};
